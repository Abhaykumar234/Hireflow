[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = ["cd hireflow-backend/hireflow-backend && ./mvnw clean package -DskipTests"]

[start]
cmd = "cd hireflow-backend/hireflow-backend && java -jar target/*.jar"